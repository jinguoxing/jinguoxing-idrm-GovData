# 识别结果确认组件拆分进度

## 当前状态

### ✅ 已完成
1. **IdentificationOverviewTab.tsx** - 已创建（概览标签页）
2. **IdentificationResultView.tsx** - 已创建（主组件框架）

### ⏳ 待完成
1. **IdentificationComparisonTab.tsx** - 需要提取（识别结果对比页，约537行）
2. **BatchConfirmationTab.tsx** - 需要提取（批量确认页，约232行）
3. **ConflictExplanationTab.tsx** - 需要提取（冲突解释页，约461行）
4. **更新 discovery/index.ts** - 添加导出
5. **更新 tt5.tsx** - 移除原代码，添加导入

## 下一步操作

### 1. 创建剩余子组件文件
从 `tt5.tsx` 中提取：
- `IdentificationComparisonTab` (第6302-6836行)
- `BatchConfirmationTab` (第6839-7068行)
- `ConflictExplanationTab` (第7071-7730行)

### 2. 更新主组件
在 `IdentificationResultView.tsx` 中：
- 导入所有子组件
- 取消注释子组件使用代码

### 3. 更新导出文件
在 `src/components/semantic-layer/views/discovery/index.ts` 中添加：
```typescript
export { IdentificationResultView } from './IdentificationResultView';
```

### 4. 更新 tt5.tsx
- 删除原组件定义（第5762-7730行）
- 在文件顶部添加导入：
```typescript
import { IdentificationResultView } from './src/components/semantic-layer/views/discovery';
```
- 更新使用处，确保传递 `GenerateBusinessObjectWizard` 组件

## 注意事项

1. **GenerateBusinessObjectWizard** 依赖：
   - 当前仍在 `tt5.tsx` 中（第5225行开始）
   - 需要作为 prop 传递给 `IdentificationResultView`
   - 或提取为独立组件后导入

2. **模拟数据**：
   - `identificationResults` 的完整 mock 数据需要从原文件复制
   - 当前主组件中只包含了一个示例数据

3. **依赖的图标和组件**：
   - 确保所有 `lucide-react` 图标正确导入
   - 确保所有 React hooks 正确导入

## 参考文件位置

- 原始代码：`tt5.tsx` 第5762-7730行
- 目标目录：`src/components/semantic-layer/views/discovery/`
- 导出文件：`src/components/semantic-layer/views/discovery/index.ts`
